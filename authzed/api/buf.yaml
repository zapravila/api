---
version: "v1"
name: "buf.build/authzed/api"
lint:
  ignore:
    - "authzed/api/v0"  # legacy from before we used buf
breaking:
  use:
    - "WIRE_JSON"
deps:
  - zapravila.build:8099/bufbuild/protovalidate
  - zapravila.build:8099/grpc-ecosystem/grpc-gateway
  - zapravila.build:8099/googleapis/googleapis
